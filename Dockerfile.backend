FROM python:3.12-slim
WORKDIR /app
COPY backend .
RUN pip install --no-cache-dir .
CMD ["sh", "-c", "python -m alembic -c alembic.ini upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"]
