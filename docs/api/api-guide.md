# API guide

Base URL is `/api/v1`. Every response includes `X-Request-ID`. Use `Authorization: Bearer <access_token>` for protected calls. Lists accept `q`, `category`, `audience`, `resource_type`, `page`, and `page_size`. Swagger is at `/docs`.
